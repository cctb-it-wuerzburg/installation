#!/bin/bash

cat<<EOF

******************************************
* First step                             *
******************************************

First we have to install a plan Ubuntu LTS server

besides the default user genomics I need to install OpenSSH-server to enable access via SSH

This can be done by using the scripts provided by https://github.com/greatfireball/create-ubuntu-autoinstall-medium


EOF
